# Подробности

Всё, что не нужно знать, чтобы просто пользоваться. Здесь — сущности и действия для
автоматизаций, приём звонков по SIP, распознавание лиц и заметки для разработки.

Обычному человеку достаточно [README](../README.md).

---

## Сущности

- `camera.*` — по одной на каждое устройство с камерой. Двери включены по умолчанию,
  обычные камеры создаются выключенными (включаются одним кликом).
- `button.*_open` — кнопка открытия для каждой двери.
- `event.*_doorbell` — звонок в дверь (тип события `ring`).
- `binary_sensor.*_call_active` — идущий вызов. Снимается автоматически через 120 с,
  если звонок не завершился явно.
- `switch.*_sip` — приём звонков по SIP (по умолчанию **выключен**, см. ниже).
- `sensor.*_sip_status` — что делает SIP-клиент прямо сейчас.

Двери без камеры получают только кнопку открытия и сущности звонка.

Новые двери и камеры появляются сами при очередном опросе. А вот **переименование**
устройства попадёт в Home Assistant только после перезагрузки интеграции.

---

## Действия

| Действие | Что делает |
|---|---|
| `loki.open_door` | Открывает дверь по её идентификатору |
| `loki.simulate_ring` | Порождает событие звонка — для проверки уведомлений без реального звонка |
| `loki.hangup` | Завершает активный вызов |
| `loki.capture_frame` | Берёт один кадр из видеопотока и делает его текущей картинкой камеры |

---

## Приём звонков по SIP

Домофон звонит на SIP-аккаунт квартиры. Loki может слушать этот аккаунт и превращать
звонок в событие Home Assistant — тогда уведомление с видео и кнопкой «Открыть»
приходит от настоящего звонка, а не от `simulate_ring`.

**Только сигнализация, без звука.** Loki узнаёт, что звонят, и от кого — но медиа не
согласовывает и разговаривать с гостем через него нельзя. Так и задумано на этом
этапе: чтобы открыть дверь, звук не нужен (открытие идёт REST-вызовом, не тоном DTMF),
а без аудио интеграция обходится без внешних зависимостей — `requirements: []`, свой
asyncio-клиент вместо нативного стека. Голосовая связь запланирована и потребует
именно этого стека.

**Переключатель `switch.*_sip` выключен по умолчанию, и это не осторожность ради
осторожности.** У аккаунта один адрес (AOR), и на нём уже может быть зарегистрирован
телефон жильца. Вытеснение его привязки со стороны телефона выглядит никак: он просто
перестаёт звонить, без ошибок и уведомлений. Поэтому:

- Прежде чем регистрироваться, Loki **смотрит**, кто на аккаунте уже есть, и при чужой
  привязке **не регистрируется вовсе** (`sensor.*_sip_status` = `blocked`).
- Перед самой первой регистрацией он около десяти минут наблюдает за аккаунтом:
  одна пустая выборка ничего не доказывает — спящий телефон выглядит так же
  (`sensor.*_sip_status` = `baseline`).
- Первая регистрация берётся на минуту, чтобы ошибка вылечилась сама.
- После регистрации Loki перепроверяет, что ничего чужого не исчезло. Если исчезло —
  снимает свою привязку и останавливается (`evicted`), а в «Настройки → Система →
  Ремонт» появляется карточка с объяснением.
- Loki **никогда не отвечает на звонок** (`200 OK`) и никогда не шлёт `6xx`: и то и
  другое отняло бы звонок у телефона жильца. Максимум — `180 Ringing`, а отказ идёт
  как `486 Busy Here`, то есть только по своей ветке.

Выключение переключателя действует немедленно и не требует перезагрузки интеграции —
это кнопка отката в один тап. Повторное включение снимает защёлку, если клиент
остановился окончательно.

**При остановке Loki отдаёт привязку обратно** — выключением переключателя, при
выгрузке интеграции и при выключении самого Home Assistant. Без этого каждый
перезапуск оставлял на аккаунте живую привязку, следующий процесс читал её как чужое
устройство и отказывался регистрироваться, пока она не истечёт сама — до пяти минут
без звонков после каждого обновления.

**`blocked` — не тупик.** Если аккаунт занят, Loki не регистрируется, но и не сдаётся:
он смотрит снова примерно тогда, когда чужая привязка должна истечь, и дальше всё
реже. Как только аккаунт освободится — зарегистрируется сам, без ручного дёргания
переключателя. Сколько осталось ждать, видно в атрибутах `sensor.*_sip_status`:
`foreign_expires_in` — сколько живёт чужая привязка, `foreign_where` — где она сидит:
по нашему публичному адресу, по нашему локальному или по чужому. Первые два почти
наверняка означают наш же след от прошлого запуска, а не чужой телефон.

**Заведите для Home Assistant отдельный номер** — тогда SIP-аккаунт будет свой,
и весь этот риск исчезает.

Дверь, с которой идёт звонок, определяет сервер по SIP-адресу звонящего. Loki не
угадывает её по названию: результат решает, какую дверь откроет кнопка в уведомлении.

---

## Уведомление с видео и кнопкой «Открыть»

При звонке интеграция публикует событие `loki_event` с `type: call_incoming`.
Готовый blueprint отправляет на телефон actionable-уведомление со снимком с камеры
двери и кнопкой «Открыть»:

[![Импортировать blueprint в Home Assistant](https://my.home-assistant.io/badges/blueprint_import.svg)](https://my.home-assistant.io/redirect/blueprint_import/?blueprint_url=https%3A%2F%2Fgithub.com%2Fwilliam-aqn%2Fhome-assistant-domofon-hacs%2Fblob%2Fmain%2Fblueprints%2Fautomation%2Floki%2Fring_notify_open.yaml)

HACS не скачивает blueprint'ы (такой категории у него нет) — импортируйте по ссылке выше.

Проверить всю цепочку без реального звонка: **Инструменты разработчика → Действия →
`loki.simulate_ring`**, указать id двери.

Для автоматизаций надёжнее триггер по шине `loki_event`, а не по состоянию
`event.*_doorbell`: event-сущность восстанавливает состояние после перезапуска
Home Assistant и на старте покажет данные прошлого звонка.

Полезная нагрузка события:

```yaml
event_type: loki_event
data:
  type: call_incoming        # call_incoming | call_ended
  device_id: 695
  device_name: Центральный вход
  entity_id: camera.loki_tsentralnyy_vkhod   # null, если камеры нет
  sip_uri: null              # заполняется, когда появится SIP
  call_id: sim-…
```

---

## Мессенджеры с кнопками

Для Telegram и ВКонтакте есть отдельные блупринты — каждый со своим, потому что
общего у них только замысел. Подпись в одном зовётся `caption`, в другом `message`;
кнопки в одном список списков, в другом словарь с рядами; идентификатор сообщения —
`message_id` против `conversation_message_id`. Параметризовать это значило бы
получить нечто, что ломается на обоих.

| | Telegram | ВКонтакте |
|---|---|---|
| Откуда картинка | Home Assistant скачивает сам у себя — годится `127.0.0.1` | забирают серверы VK, нужен адрес **снаружи** |
| Без внешнего адреса | — | уйдёт текст с кнопками, но без фото |
| Снять кнопки, если не нажали | по `message_id` из ответа службы | по `conversation_message_id` из ответа службы |
| Ответ на нажатие | `answer_callback_query` | `answer_callback`, обязателен — иначе кружок крутится |

Обе кнопки привязаны к запуску, а не к двери: полезная нагрузка несёт
идентификатор прогона, и ждёт её только он. Кнопка из вчерашней переписки не
откроет дверь сегодня.

Ожидание нажатия живёт в действиях, а не в триггере автоматизации, и это
вынужденно: `event_data` триггера вычисляется один раз при загрузке, шаблон с
идентификатором запуска туда не подставить. В `wait_for_trigger` он рендерится
против переменных запуска — на этом всё и держится.

Просто текст без кнопок ни того ни другого блупринта не требует:
`vk_notify.send_message` и любая другая служба, принимающая `message`, вписывается
в «Озвучить звонок».

---

## Ссылки, которые понимает страница «Домофоны»

| Адрес | Что показывает |
|---|---|
| `/loki` | все двери плитками |
| `/loki/<id>` | одну дверь: картинка и кнопка «Открыть» |
| `/loki/<id>?live=1` | то же, но сразу с живым видео |

`<id>` — это `device_id` из события `loki_event`. Эти же адреса годятся для
`clickAction` в уведомлении на телефон и для любой своей кнопки на дашборде.

---

## Карточки на дашборд

Интеграция приносит две готовые карточки. Ставить и подключать их отдельно не нужно —
они появляются сами в списке добавления. YAML писать не нужно нигде.

При первой настройке интеграция предлагает добавить в боковое меню готовую страницу
**«Домофоны»** — все двери плитками с кнопкой открытия под каждой. Это отдельный
пункт меню, ваши дашборды он не трогает; включается и выключается в настройках
интеграции.

Карточки при этом всё равно доступны и их можно положить на свой дашборд.

### Куда нажимать

⚠️ **Дашборд «Обзор» генерируется автоматически, и добавить в него ничего нельзя** —
там просто нет кнопки «Добавить карточку». Это относится не к Loki, а вообще ко всему:
пока дашбордом управляет Home Assistant, редактировать его нельзя.

Есть два выхода.

**Проще и безопаснее — отдельный дашборд.** Настройки → Панели мониторинга →
**Добавить панель** → «Новая панель с нуля». Открыть её, **карандаш** справа сверху →
**Добавить карточку** → в поиске набрать `Loki`.

**Или взять «Обзор» под свой контроль.** Открыть «Обзор», карандаш справа сверху,
Home Assistant предупредит, что после этого перестанет обновлять дашборд сам, и
новые устройства придётся добавлять руками. Это необратимо.

После обновления интеграции **обновите страницу один раз** (Ctrl+Shift+R): список
подключаемых модулей применяется при загрузке страницы, а файл карточек браузер
кеширует надолго.

### Loki — домофон

Пока идёт вызов карточка выглядит иначе: под именем двери появляется «Входящий
вызов», счётчик времени и полоса, которая пустеет к концу звонка, а рядом с
«Открыть» — круглая красная **«Завершить»**. Она убирает звонок из Home Assistant
и освобождает нашу SIP-ветку; домофон при этом **продолжает звонить на телефон
жильца** — отменить вызов целиком отсюда нельзя и не нужно.

Раскладок две. Обычная: широкая карточка ставит картинку и кнопки в две колонки,
узкая — друг под другом (это же и вертикальная панель). Компактная (`layout:
compact`) — одна строка: миниатюра, кто и сколько, и две круглые кнопки.

Полоса времени рассчитана на 30 секунд — столько обычно длится звонок в дверь.
Это не таймаут интеграции (тот 120 с и служит страховкой на случай, когда о конце
вызова никто не сообщил), поэтому пустая полоса ничего не завершает. Меняется в
настройках карточки.


Одна дверь: картинка с камеры, кнопка **«Открыть»** поверх неё, отметка «Вызов», когда
кто-то звонит, и предупреждение, когда живое видео недоступно, — чтобы не гадать,
показан живой кадр или последний снимок.

В настройках карточки один обязательный пункт — **какой домофон**. Список сразу
отфильтрован: в нём только двери Loki и ничего лишнего. Кнопку открытия и датчик
вызова карточка находит сама.

Кнопка **видеокамеры** в углу картинки включает живой поток на этой двери и через
заданное время выключает его обратно.

### Loki — все домофоны

Плитки всех домофонов сразу, под каждой — кнопка открытия. Нужна для случая «человек
где-то стоит, надо найти глазами и открыть именно ту дверь».

По умолчанию плитки показывают снимки: два десятка живых потоков 1080p — это несколько
загруженных ядер и десятки мегабит постоянного трафика с чужого сервера. Кнопка
**«Все вживую»** переводит все плитки в живое видео разом, и через **30 секунд** они
сами возвращаются к снимкам. Этого хватает, чтобы пробежать глазами по дверям, и
недостаточно, чтобы, отойдя от планшета, оставить два десятка потоков висеть. Нажать
можно снова; в настройках карточки время меняется. Уйти со страницы тоже достаточно —
потоки останавливаются сами.

Отдельную плитку можно включить вживую поодиночке, не поднимая остальные.

### Картинка на плитке и «Текущий кадр» — разные вещи

Обычная картинка на плитке приходит **с сервера оператора и статична**: проверено —
три запроса за полторы минуты вернули побайтово одинаковый JPEG. Это узнаваемый вид
двери, но не то, что происходит сейчас.

Кнопка **«Текущий кадр»** берёт **один кадр из видеопотока** каждого домофона и
делает его картинкой на плитке. Это и есть «что там сейчас», и стоит это несравнимо
дешевле непрерывного просмотра: одно короткое подключение вместо постоянного.

То же самое доступно из автоматизаций — действие `loki.capture_frame`.

**Когда видеопоток недоступен, обе кнопки заблокированы** — и «Текущий кадр», и «Все
вживую». Кадр брать неоткуда, а нажатие открывало бы соединения, про которые уже
известно, что они отвалятся по таймауту, и заваливало бы журнал ошибками.

Статичная картинка с сервера при этом продолжает показываться: она идёт по другому
пути и с другого хоста.

В настройках карточки — список сущностей, который можно отредактировать: убрать
лишние двери, поменять порядок. Там же **размер плиток**: мелкие, средние или
крупные. Число плиток в ряду считается от ширины карточки, а не задаётся жёстко —
на телефоне и на мониторе получится по-разному, и это правильно. Если нужно ровно
столько-то колонок, есть отдельная настройка.

**Чтобы карточка занимала всю ширину страницы**, ширину задаёт не она, а вид
дашборда. В виде с секциями откройте настройки секции и включите **«Занимать всю
ширину»** (`column_span` на число колонок вида). Либо положите карточку в вид типа
**«Панель»** — он отдаёт одной карточке всю страницу целиком.

Плитки при этом сами разложатся по ширине: их число в ряду считается от места, а не
задано жёстко.

### Если карточек нет в списке

Файл отдаётся самой интеграцией по адресу `/loki_cards/loki-cards.js`, и она сама
прописывает его в ресурсы дашборда — руками добавлять ничего не надо. По порядку:

1. Обновите страницу с очисткой кеша (Ctrl+Shift+R).
2. Проверьте, что вы **не** на автогенерируемом дашборде — см. выше.
3. Откройте `/loki_cards/loki-cards.js` в браузере: если файл открывается, а карточек
   нет, значит страница загрузилась до того, как интеграция запустилась — перезапустите
   Home Assistant.

Известные ограничения, оба не лечатся: карточки **не грузятся в безопасном режиме**
(Home Assistant там отключает все сторонние модули) и **не работают в Home Assistant
Cast**, который не загружает страницу, куда они подключаются.

---

## Распознавание лиц (Frigate)

Интеграция сама лица не распознаёт — она использует Frigate и лишь связывает его
события с конкретной дверью домофона. Ключевое:

- **Не направляйте Frigate на все камеры.** Узкое место — декодирование, а не детекция,
  и `detect: fps` его не снижает. Хватит 1–2 главных дверей.
- **Только через go2rtc** (встроен в Frigate): одно исходящее соединение к облаку
  разделяется между детекцией, записью и просмотром.
- Распознавание лиц требует CPU с **AVX+AVX2** и не ускоряется Google Coral (Coral —
  только детекция объектов). Для точной модели нужен Intel iGPU (OpenVINO) или GPU.
- Требуется Frigate **0.17.2+**.

> Автоматическое открытие двери по распознанному лицу намеренно не поддерживается:
> ни один движок не выполняет проверку живости, а подъездная дверь — общее имущество.
> Blueprint по лицу уведомляет, но не открывает.

---

## Разработка

```bash
pip install ruff pytest pytest-homeassistant-custom-component pyyaml
ruff check custom_components tests scripts
python scripts/validate_yaml.py
pytest tests -q
```

Быстрые тесты (`test_models.py`, `test_protocol.py`) не требуют установленного
Home Assistant — они проверяют разбор данных и правила протокола.

---

## Проверенные версии

Разрабатывалось и проверялось на Home Assistant **2026.8.2**. Минимальная заявленная
версия — 2025.12; на более старых интеграция должна работать, но карточки могут
выглядеть иначе, потому что раскладка дашбордов с тех пор менялась.

## Планы

[Распознавание лиц через Frigate](PHASE4_FRIGATE.md) — что сделано, что осталось и
какие решения уже приняты.
